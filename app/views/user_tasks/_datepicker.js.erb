$(document).ready(function(){
  $('#user_task_due input').datepicker({
    autoclose: true,
    todayHighlight: true,
    toggleActive: true,
    format: "yyyy-mm-dd",
    orientation: "top auto"
  }); //sandbox-container
}); //(document).ready